import React from "react";


export default function Header({pageList, currentPage, handlePage}) {

    return (
        <header class="col-6 p-3">
            <h1>Max McDonough</h1>
        </header>
    );
};